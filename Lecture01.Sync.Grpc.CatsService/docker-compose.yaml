services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: cats_grpc
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5433:5432"
    volumes:
      - cats-grpc-data:/var/lib/postgresql/data

volumes:
  cats-grpc-data:
